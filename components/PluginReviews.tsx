"use client";
import {useEffect,useMemo,useState} from 'react';import {BadgeCheck,LockKeyhole,Star} from 'lucide-react';import {supabase} from '@/lib/supabase';
export default function PluginReviews({pluginId,owned,signedIn}:{pluginId:string;owned:boolean;signedIn:boolean}){const [reviews,setReviews]=useState<any[]>([]),[rating,setRating]=useState(5),[feedback,setFeedback]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const avg=useMemo(()=>reviews.length?reviews.reduce((a,r)=>a+Number(r.rating),0)/reviews.length:0,[reviews]);
async function load(){const r=await fetch(`/api/reviews/${pluginId}`,{cache:'no-store'});const b=await r.json();setReviews(b.reviews||[]);if(supabase){const u=(await supabase.auth.getUser()).data.user;const mine=(b.reviews||[]).find((x:any)=>x.user_id===u?.id);if(mine){setRating(mine.rating);setFeedback(mine.feedback)}}}useEffect(()=>{load()},[pluginId]);
async function submit(){if(!supabase)return;setBusy(true);setMsg('');const t=(await supabase.auth.getSession()).data.session?.access_token;const r=await fetch(`/api/reviews/${pluginId}`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${t}`},body:JSON.stringify({rating,feedback})});const b=await r.json();setBusy(false);if(!r.ok)return setMsg(b.error||'Could not submit review.');setMsg('Your review has been saved.');load()}
return <section className="pluginReviews"><div className="reviewsHeader"><div><span>CUSTOMER FEEDBACK</span><h2>Ratings & Reviews</h2><p>Ratings and reviews can only be submitted by customers who have purchased this plugin.</p></div><div className="ratingSummary"><strong>{reviews.length?avg.toFixed(1):'—'}</strong><div><div className="starsStatic">{[1,2,3,4,5].map(n=><Star key={n} size={15} fill={n<=Math.round(avg)?'currentColor':'none'}/>)}</div><span>{reviews.length} review{reviews.length===1?'':'s'}</span></div></div></div>
<div className="reviewComposer">{!signedIn?<div className="reviewLocked"><LockKeyhole/> Sign in to see whether you can review this plugin.</div>:!owned?<div className="reviewLocked"><LockKeyhole/> Only customers who have purchased this plugin can rate or write a review.</div>:<><div className="starPicker"><span>Your rating</span>{[1,2,3,4,5].map(n=><button key={n} onClick={()=>setRating(n)} aria-label={`${n} stars`}><Star fill={n<=rating?'currentColor':'none'}/></button>)}</div><textarea value={feedback} maxLength={2000} onChange={e=>setFeedback(e.target.value)} placeholder="Share your experience with this plugin…"/><div className="reviewComposerBottom"><span>{feedback.length}/2000</span><button className="primaryBtn" disabled={busy} onClick={submit}>{busy?'Saving…':'Submit / Update Review'}</button></div>{msg&&<p className="fieldHelp">{msg}</p>}</>}</div>
<div className="reviewList">{reviews.length?reviews.map(r=>{const pr=Array.isArray(r.profiles)?r.profiles[0]:r.profiles;return <article className="reviewCard" key={r.id}><div className="reviewAvatar">{pr?.avatar_url?<img src={pr.avatar_url}/>:String(pr?.nickname||'A').slice(0,1).toUpperCase()}</div><div className="reviewBody"><div className="reviewAuthor"><strong>{pr?.nickname||'Aevon User'} {pr?.verification_status==='verified'&&<BadgeCheck className="verifiedBadge" size={15}/>}</strong><span className="ownerBadge">Verified Owner</span><time>{new Date(r.created_at).toLocaleDateString()}</time></div><div className="starsStatic">{[1,2,3,4,5].map(n=><Star key={n} size={14} fill={n<=r.rating?'currentColor':'none'}/>)}</div><p>{r.feedback}</p></div></article>}):<div className="noReviews">No reviews yet. Owners of this plugin can be the first to share feedback.</div>}</div></section>}
